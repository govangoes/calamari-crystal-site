name: CI

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install deps (prefer npm ci, fallback to npm install)
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund

      - name: Optimize images (WebP gen)
        run: npm run img:opt

      - name: Show workspace (debug)
        run: |
          node -v
          npm -v
          ls -la
          echo "---- src tree ----"
          ls -laR src || true

      - name: Lint
        run: npm run lint || echo "Skipping lint (not configured on this branch)"

      - name: Test
        run: npm test -- --run || echo "Skipping tests if none configured"

      - name: Build
        run: npm run build

      - name: Upload dist artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
